from fastapi import APIRouter
from app.services.prediction_service import predict_stock

router = APIRouter()

@router.get("/{symbol}")
async def get_prediction(symbol: str):
    """
    Returns prediction payload generated by the local LSTM Model engine.
    """
    return await predict_stock(symbol)
